"use strict"

class Weapon {
    constructor() {
    this.name = name;
    this.attack = attack;
    this.durability = urability;
    this.range = range;
    }

    takeDamage(damage) {
        Weapon.durability -= damage;
        if (Weapon.durability <= 0) {
            Weapon.durability === 0;
            console.log(Weapon.durability);
        } else {
            console.log(Weapon.durability);
        }
    }
    getDamage() {
        
    }
}